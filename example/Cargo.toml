[package]
name = "example"
version = "0.1.0"
edition = "2021"

[[bench]]
harness = false
name = "i18n_bench"

[dependencies]
ftl-core = { path = "../core" }

[dev-dependencies]
criterion = "0.8"

[build-dependencies]
ftl-codegen = { path = "../codegen" }
